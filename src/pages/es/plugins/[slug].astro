---
import { site } from '@data/site.ts';

export async function getStaticPaths() {
  return site.plugins.map((plugin) => ({
    params: { slug: plugin.slug },
    redirect: `/es/open-source/${plugin.slug}/`,
  }));
}
---
